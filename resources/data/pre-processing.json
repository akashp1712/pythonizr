{
  "name": "Data",
  "data": {
    "dataLoading": {
      "name": "Data Loading",
      "imports": ["import pandas as pd"],
      "code": [
        "# Load red wine data.",
        "dataset_url = '' #TODO: set the url or local reference",
        "data = pd.read_csv(dataset_url, sep=';')",
        "y = data.FEATURE_NAME #TODO: repalce FEATURE_NAME with the one you want to predict",
        "X = data.drop('FEATURE_NAME', axis=1) #TODO: remove the FEATURE_NAME from the data"
      ]
    },
    "normalization": {
      "name": "Normalization",
      "imports": ["from sklearn.preprocessing import scale"],
      "code": [
        "X = scale(X)"
      ]
    },
    "trainTestSplit": {
      "name": "Train Test Splitting",
      "imports": ["from sklearn.model_selection import train_test_split"],
      "code": [
        "# Split data into training and test sets",
        "X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2, random_state=123, stratify=y)"
      ]
    }
  }
}
